#include <stdio.h>

int main()
{

    //Arithmetic operators
    // ( + , - , * , / , % )

    //Eaquality / Relational operators
    // ( == , != , > , < , >= , <= )

    //Logical / Conditional operators
    // ( ! , && , || )

    //Bitwise operators
    // ( & , | , ^ , << , >> , ~ )

    //Assignment operators
    // ( = , += , -= , *= , /= , %= )

    return 0;
}
